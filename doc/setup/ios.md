# Toolkit

1. [Homebrew](https://brew.sh/)

# Preparing

- Before your first run, it's crucial to open the [workspace in ios folder](../../ios/Runner.xcworkspace) using Xcode.
- Run given commands:

```shell
brew install cmake ninja cocoapods flutter

flutter pub get

cd ios && pod install
```

